{
  "name": "@dananeh/functions",
  "version": "1.0.0",
  "private": true,
  "description": "Server-authoritative publishing and progress ingestion.",
  "main": "lib/index.bundle.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && node ../scripts/bundle-functions.mjs",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "firebase-admin": "13.6.0",
    "firebase-functions": "6.6.0"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
